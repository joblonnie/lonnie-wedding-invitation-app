.DS_Store
node_modules
dist
dist-ssr
coverage
.env
.env.local
.env.*.local

# Yarn Berry (no zero-installs)
.pnp.*
.yarn/cache
.yarn/unplugged
.yarn/build-state.yml
.yarn/install-state.gz

# keep these committed if present
!.yarn/releases
!.yarn/plugins
!.yarn/sdks
!.yarn/versions

# Next.js
.next/
next-env.d.ts
